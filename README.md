# CryptoKit (CK) - Simple Cryptography Toolkit

A clean, easy-to-use command-line cryptography toolkit for encryption, hashing, steganography, and security analysis.

![License](https://img.shields.io/badge/license-MIT-blue.svg)
![Python](https://img.shields.io/badge/python-3.8%2B-brightgreen)
![Platform](https://img.shields.io/badge/platform-Linux%20%7C%20Windows%20%7C%20macOS-lightgrey)

## Features

- 🔐 **Symmetric Encryption**: AES-128 and 3DES algorithms
- 🏷️ **Cryptographic Hashing**: MD5, SHA1, SHA256, SHA512, BLAKE2B (5 algorithms by default)
- 🖼️ **Steganography**: Hide data in images using LSB method
- 📊 **Metadata Analysis**: File analysis and security scanning
- 🎯 **Interactive Mode**: Numbered menu options (1-7) for easy navigation
- ⚡ **Simple Commands**: Short aliases (-e, -d, -h, -i) for quick access

## Quick Start

### Installation

```bash
# Clone the repository
git clone https://github.com/yourusername/CK.git
cd CK

# Install dependencies
pip install -r requirements.txt

# Install the package
pip install -e .
```

### Basic Usage

```bash
# Start interactive mode (numbered options)
ck -i

# Encrypt a file (creates demoEncrypt.txt)
ck -e demo.txt

# Decrypt a file (creates demoDecrypt.txt)
ck -d demoEncrypt.txt

# Generate file hashes (5 algorithms by default)
ck -h demo.txt

# Analyze file metadata
ck metadata demo.txt
```

## Commands

### Interactive Mode

Start the interactive mode with numbered options for easy navigation:

```bash
ck -i
```

**Interactive Menu:**
```
CryptoKit (CK) - Interactive Mode
Select an option:

1. Encrypt a file
2. Decrypt a file
3. Generate file hash
4. Hide data in image (steganography)
5. Extract data from image
6. Analyze file metadata
7. Exit

Enter your choice (1-7):
```

### Command Line Options

- `ck -i` or `ck --interactive` - Start interactive mode
- `ck -e <file>` - Encrypt a file
- `ck -d <file>` - Decrypt a file  
- `ck -h <file>` - Generate file hashes
- `ck --algo <algorithm>` - Specify encryption algorithm (aes-128, 3des)

### Examples

#### Encryption
```bash
# Encrypt demo.txt (creates demoEncrypt.txt)
ck -e demo.txt

# Use 3DES algorithm instead of default AES-128
ck -e demo.txt --algo 3des
```

When encrypting, you'll be prompted for a password. The tool automatically:
- Creates an encrypted file with "Encrypt" suffix (demo.txt → demoEncrypt.txt)
- Generates a key file (Key_demo.txt)

#### Decryption
```bash
# Decrypt demoEncrypt.txt (creates demoDecrypt.txt)
ck -d demoEncrypt.txt
```

For decryption, you need the key file that was created during encryption.

#### Hashing
```bash
# Generate hashes with 5 algorithms by default
ck -h demo.txt
```

This creates `demoHash.txt` containing:
- MD5
- SHA1
- SHA256
- SHA512
- BLAKE2B

#### Steganography
```bash
# Hide data in image (interactive mode recommended)
ck -i
# Then select option 4
```

#### Metadata Analysis
```bash
# Analyze file metadata
ck metadata demo.txt

# Detailed analysis
ck metadata suspicious_file.exe
```

## Smart File Naming

The tool uses intelligent file naming conventions:

- **Encryption**: `demo.txt` → `demoEncrypt.txt`
- **Decryption**: `demoEncrypt.txt` → `demoDecrypt.txt`
- **Hashing**: `demo.txt` → `demoHash.txt`
- **Key files**: `demo.txt` → `Key_demo.txt`

## Supported Algorithms

### Encryption
- **AES-128** (default) - Fast and secure
- **3DES** - Legacy compatibility

### Hashing (All generated by default)
- **MD5** - Fast checksum
- **SHA1** - Legacy support
- **SHA256** - Current standard
- **SHA512** - Enhanced security
- **BLAKE2B** - Modern algorithm

## Installation on Kali Linux

```bash
# Update system
sudo apt update

# Install Python pip if needed
sudo apt install python3-pip

# Clone and install
git clone https://github.com/yourusername/CK.git
cd CK
pip install -r requirements.txt
pip install -e .

# Verify installation
ck -i
```

## File Structure

```
CK/
├── ck/                     # Main package
│   ├── cli/               # Command line interface
│   ├── algorithms/        # Crypto algorithms
│   ├── services/          # Core services
│   └── core/              # Configuration & utilities
├── config/                # Configuration files
├── requirements.txt       # Dependencies
└── setup.py              # Installation script
```

## Output Examples

### Encryption Output
```
$ ck -e demo.txt
Enter encryption password: [hidden]
Confirm password: [hidden]
Encrypting demo.txt with AES-128...
✓ Encryption successful!
  Encrypted file: demoEncrypt.txt
  Key file: Key_demo.txt
```

### Hash Output
```
$ ck -h demo.txt
Generating hashes for demo.txt...
✓ Hash generation complete!
  Hash file: demoHash.txt
  Algorithms: MD5, SHA1, SHA256, SHA512, BLAKE2B
```

### Interactive Mode Output
```
$ ck -i
CryptoKit (CK) - Interactive Mode
Select an option:

1. Encrypt a file
2. Decrypt a file
3. Generate file hash
4. Hide data in image (steganography)
5. Extract data from image
6. Analyze file metadata
7. Exit

Enter your choice (1-7): 1
Enter file to encrypt: demo.txt
Select algorithm:
1. AES-128 (recommended)
2. 3DES
Enter choice (1-2): 1
Enter encryption password: [hidden]
✓ Encryption successful!
```

## Troubleshooting

### Common Issues

**Command not found:**
```bash
# Ensure proper installation
pip install -e .

# Check if ck is in PATH
which ck
```

**Permission errors:**
```bash
# Ensure write permissions in current directory
chmod +w .

# Or specify output directory you have access to
```

**Import errors:**
```bash
# Reinstall dependencies
pip install -r requirements.txt
```

## Contributing

1. Fork the repository
2. Create a feature branch (`git checkout -b feature/new-feature`)
3. Commit your changes (`git commit -m 'Add new feature'`)
4. Push to the branch (`git push origin feature/new-feature`)
5. Open a Pull Request

## License

This project is licensed under the MIT License - see the [LICENSE](LICENSE) file for details.

## Security Notice

This tool is designed for educational and authorized testing purposes only. Users are responsible for ensuring compliance with applicable laws and regulations.
